<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks cleartext HTTP and WS by default from API 28 onwards. Without
  an exemption, `ws://10.0.2.2:8080` and `http://10.0.2.2:8080` are refused
  before a packet leaves the device, and the error surfaces as a generic
  connection failure - which is a miserable thing to debug.

  The blunt fix is `android:usesCleartextTraffic="true"` on <application>. This
  file is the scoped alternative, and the reason to prefer it is that it does
  not disable the protection for every other host the app might ever talk to:
  cleartext stays blocked except for the three addresses that mean "the machine
  running the dev server".

    10.0.2.2   - the Android emulator's alias for the host machine
    localhost  - for `adb reverse tcp:8080 tcp:8080` on a physical device
    127.0.0.1  - the same, spelled numerically

  `cleartextTrafficPermitted="false"` on the base config restates the platform
  default explicitly, so the intent is legible rather than inherited.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
